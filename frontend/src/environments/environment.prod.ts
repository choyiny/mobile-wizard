export const environment = {
  production: true,
  backend: 'http://localhost:8000/',
  peerserver: {
    id: 'asdf', // blank string for random room id
    host: 'peer.choy.in',
    port: 443,
    key: 'peerjs',
  }
};
